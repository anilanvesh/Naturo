<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Base application theme -->
    <style name="Theme.Naturo" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color -->
        <item name="colorPrimary">@color/primary</item>
        <item name="colorPrimaryVariant">@color/primary_variant</item>
        <item name="colorOnPrimary">@color/on_primary</item>

        <!-- Secondary brand color -->
        <item name="colorSecondary">@color/secondary</item>
        <item name="colorSecondaryVariant">@color/secondary_variant</item>
        <item name="colorOnSecondary">@color/on_secondary</item>

        <!-- Status bar color -->
        <item name="android:statusBarColor">?attr/colorPrimaryVariant</item>

        <!-- Background colors -->
        <item name="android:windowBackground">@color/background</item>
        <item name="colorSurface">@color/surface</item>
        <item name="colorOnSurface">@color/on_surface</item>
        <item name="colorError">@color/error</item>
        <item name="colorOnError">@color/on_error</item>

        <!-- Text colors -->
        <item name="android:textColorPrimary">@color/on_surface</item>
        <item name="android:textColorSecondary">@color/on_background</item>

        <!-- Widget styles -->
        <item name="buttonStyle">@style/Widget.Naturo.Button</item>
        <item name="switchStyle">@style/Widget.Naturo.Switch</item>
        <item name="cardViewStyle">@style/Widget.Naturo.Card</item>

        <!-- Custom attributes -->
        <item name="soundItemBackground">@color/sound_item_active</item>
        <item name="timerTextColor">@color/timer_text</item>
    </style>

    <!-- Dark theme -->
    <style name="Theme.Naturo.Dark" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color -->
        <item name="colorPrimary">@color/primary_variant</item>
        <item name="colorPrimaryVariant">@color/primary</item>
        <item name="colorOnPrimary">@color/on_primary</item>

        <!-- Secondary brand color -->
        <item name="colorSecondary">@color/secondary_variant</item>
        <item name="colorSecondaryVariant">@color/secondary</item>
        <item name="colorOnSecondary">@color/on_secondary</item>

        <!-- Status bar color -->
        <item name="android:statusBarColor">?attr/colorPrimary</item>

        <!-- Background colors -->
        <item name="android:windowBackground">@color/background_dark</item>
        <item name="colorSurface">@color/surface_dark</item>
        <item name="colorOnSurface">@color/on_surface_dark</item>
        <item name="colorError">@color/error_dark</item>
        <item name="colorOnError">@color/on_error_dark</item>

        <!-- Text colors -->
        <item name="android:textColorPrimary">@color/on_surface_dark</item>
        <item name="android:textColorSecondary">@color/on_background_dark</item>

        <!-- Widget styles -->
        <item name="buttonStyle">@style/Widget.Naturo.Button</item>
        <item name="switchStyle">@style/Widget.Naturo.Switch</item>
        <item name="cardViewStyle">@style/Widget.Naturo.Card</item>

        <!-- Custom attributes -->
        <item name="soundItemBackground">@color/sound_item_active_dark</item>
        <item name="timerTextColor">@color/timer_text_dark</item>
    </style>

    <!-- Custom attributes for styling components like SoundItem background and Timer text color -->
    <attr name="soundItemBackground" format="color" />
    <attr name="timerTextColor" format="color" />
</resources>
